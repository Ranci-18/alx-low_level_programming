#include "main.h"
#include <stdio.h>

/**
 *add - computes sum
 *@x: is first number
 *@y: is second number
 *Returns: addition of two numbers
 */

int add(int x, int y)
{
	int sum;

	sum = x + y;
	return (sum);

}
