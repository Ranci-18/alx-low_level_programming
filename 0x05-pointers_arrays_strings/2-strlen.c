#include <stdio.h>
#include <string.h>

/**
 *_strlen - calculates length of string
 *
 *@s: pointer to the string
 *
 *Return: string length as an integer
 */
int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (length);
}
